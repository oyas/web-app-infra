apiVersion: apps/v1
kind: Deployment
metadata:
  name: application
spec:
  replicas: 1
  selector:
    matchLabels:
      app: application
  template:
    metadata:
      labels:
        app: application
      annotations:
        'consul.hashicorp.com/connect-inject': 'true'
    spec:
      containers:
      - name: application
        image: oyas/web-app-bff
        ports:
        - containerPort: 4000
        env:
        - name: API_URL_ARTICLES
          value: "application.app-dev-articles.svc.cluster.local:50052"
